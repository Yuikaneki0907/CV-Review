# Optional: set this to call the backend directly instead of using the Vite proxy.
# VITE_API_URL=http://34.21.136.67:8020/api/v1

# When running the frontend inside Docker Compose, proxy API requests to the backend service.
VITE_PROXY_TARGET=http://backend:8020
